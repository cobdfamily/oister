{
  "name": "@cobdfamily/clam",
  "version": "0.0.0",
  "private": true,
  "description": "Native-bridge toolkit for the cobdfamily super-app shell (cobdcorekit).",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "npm run build -w @cobdfamily/cobdcorekit",
    "test": "npm test --workspaces --if-present"
  },
  "workspaces": [
    "packages/*",
    "!packages/springboard"
  ]
}
