{
  "name": "@cobdfamily/cobdcorekit",
  "version": "0.0.0",
  "description": "The COBDCoreKit client API: shims navigator.geolocation and exposes COBDCoreKit.torch, routing both to cobdhostkit. The only bridge API you call, inside or outside the iframe.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  }
}
