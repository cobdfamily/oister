# icon for COBD

Drop a square **`icon.png`** here, **≥ 1024×1024**. `@capacitor/assets` generates
every iOS/Android icon (and splash) from it during regeneration. It is the only
image asset this app needs.
