{
  "$comment": "DEPRECATED for permissions. Native permissions are now generated per app from brand.json `extra.capabilities` via @cobdfamily/cobdcorekit's capability→key map + @cobdfamily/clf-core's localized perm.* strings (English fallback when clf-core isn't installed). The generator no longer reads this file. Kept as a placeholder for any future shared native config that isn't permission-driven."
}
