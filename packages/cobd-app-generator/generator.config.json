{
  "$comment": "Single source of truth for the generator. Bump capacitorVersion here to upgrade ALL apps on their next regeneration — there are no committed native projects to migrate.",
  "capacitorVersion": "8.4.0",
  "platforms": ["android", "ios"],
  "base": {
    "$comment": "The shared web shell every app wraps. NOT YET CONFIGURED — cobdappkit was removed. Point buildCommand at a web-only build and distDir at its output (the static web that becomes each app's webDir).",
    "buildCommand": "",
    "distDir": ""
  },
  "appsDir": "apps",
  "sharedOverlay": "shared/overlay.json",
  "cdnBase": "https://cdn.blindhub.ca",
  "cdnManifest": "shared/cdn.json",
  "outDir": ".generated"
}
