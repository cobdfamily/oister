{
  "name": "@cobdfamily/cobdhostkit",
  "version": "0.0.0",
  "description": "Native broker for the COBDCoreKit bridge: runs in the super-app shell and answers cobdcorekit calls with native Capacitor plugins. The only package that touches native; never called directly.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@capacitor/haptics": "^7.0.1",
    "@capgo/capacitor-flash": "^7.0.4",
    "@cobdfamily/cobdcorekit": "*"
  },
  "peerDependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "^7.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.6.3"
  }
}
